services:
  server:
    image: geekembly:builder
    volumes:
      - .:/src
    command: "./scripts/run.sh"
  kaniko:
    image: gcr.io/kaniko-project/executor:latest
    volumes:
      - .:/src
    command: tail -f /dev/null
